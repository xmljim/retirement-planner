<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
        "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
        "https://checkstyle.org/dtds/configuration_1_3.dtd">
<!--
  Retirement Planner Checkstyle config — see ADR-009.

  Style rules: standard. Project-specific rules from ADR-007 / CLAUDE.md
  expressed as RegexpSinglelineJava entries near the bottom.
-->
<module name="Checker">
    <property name="severity" value="warning"/>
    <property name="fileExtensions" value="java, properties, xml"/>

    <module name="SuppressionFilter">
        <property name="file" value="${suppressions.file}"/>
        <property name="optional" value="true"/>
    </module>

    <module name="SuppressWarningsFilter"/>

    <module name="LineLength">
        <property name="max" value="140"/>
        <property name="ignorePattern" value="^package.*|^import.*|a href|href|http://|https://|ftp://"/>
    </module>

    <module name="FileTabCharacter">
        <property name="eachLine" value="true"/>
    </module>

    <module name="RegexpSingleline">
        <property name="format" value="\s+$"/>
        <property name="minimum" value="0"/>
        <property name="maximum" value="0"/>
        <property name="message" value="Trailing whitespace"/>
    </module>

    <module name="TreeWalker">

        <!-- Annotations -->
        <module name="AnnotationLocation">
            <property name="allowSamelineMultipleAnnotations" value="false"/>
            <property name="allowSamelineSingleParameterlessAnnotation" value="false"/>
            <property name="allowSamelineParameterizedAnnotation" value="false"/>
        </module>
        <module name="MissingOverride"/>

        <!-- Imports -->
        <module name="AvoidStarImport"/>
        <module name="RedundantImport"/>
        <module name="UnusedImports"/>

        <!-- Naming -->
        <module name="ConstantName">
            <!-- Allow snake_case for test ArchRule fields (readable rule names) -->
            <property name="format" value="^([A-Z][A-Z0-9]*(_[A-Z0-9]+)*|[a-z][a-zA-Z0-9_]*)$"/>
        </module>
        <module name="LocalFinalVariableName"/>
        <module name="LocalVariableName"/>
        <module name="MemberName"/>
        <module name="MethodName">
            <!-- Allow snake_case method names in tests; standard JUnit5 convention -->
            <property name="format" value="^[a-z][a-zA-Z0-9_]*$"/>
        </module>
        <module name="PackageName">
            <property name="format" value="^[a-z]+(\.[a-z][a-z0-9]*)*$"/>
        </module>
        <module name="ParameterName"/>
        <module name="StaticVariableName"/>
        <module name="TypeName"/>

        <!-- Whitespace -->
        <module name="EmptyForIteratorPad"/>
        <module name="GenericWhitespace"/>
        <module name="MethodParamPad"/>
        <module name="NoWhitespaceAfter"/>
        <module name="NoWhitespaceBefore"/>
        <module name="OperatorWrap"/>
        <module name="ParenPad"/>
        <module name="TypecastParenPad"/>
        <module name="WhitespaceAfter"/>
        <module name="WhitespaceAround">
            <property name="allowEmptyConstructors" value="true"/>
            <property name="allowEmptyMethods" value="true"/>
            <property name="allowEmptyTypes" value="true"/>
            <property name="allowEmptyLoops" value="true"/>
            <property name="allowEmptyLambdas" value="true"/>
        </module>

        <!-- Blocks -->
        <module name="LeftCurly"/>
        <module name="NeedBraces"/>
        <module name="RightCurly"/>
        <module name="EmptyBlock">
            <property name="option" value="text"/>
        </module>

        <!-- Coding -->
        <module name="DefaultComesLast"/>
        <module name="EmptyStatement"/>
        <module name="EqualsHashCode"/>
        <module name="MissingSwitchDefault"/>
        <module name="MultipleVariableDeclarations"/>
        <module name="OneStatementPerLine"/>
        <module name="PackageDeclaration"/>
        <module name="StringLiteralEquality"/>
        <module name="UnnecessarySemicolonInEnumeration"/>
        <module name="UnnecessarySemicolonInTryWithResources"/>

        <!-- Modifiers -->
        <module name="ModifierOrder"/>
        <module name="RedundantModifier"/>

        <!-- Misc -->
        <module name="ArrayTypeStyle"/>
        <module name="OuterTypeFilename"/>
        <module name="UpperEll"/>

        <!-- ============================================================== -->
        <!-- Project-specific rules (ADR-007, ADR-008, CLAUDE.md)             -->
        <!-- ============================================================== -->

        <!-- ADR-007: `double` and `float` banned outside MC inner loop -->
        <module name="Regexp">
            <property name="format" value="\b(double|float)\b\s+\w+|\b(double|float)\[\]"/>
            <property name="message" value="ADR-007: primitive double/float forbidden. Use Money. The MC inner loop is the only sanctioned exception (ADR-005)."/>
            <property name="illegalPattern" value="true"/>
            <property name="ignoreComments" value="true"/>
        </module>

        <!-- CLAUDE.md: ban LocalDateTime.now() and zero-arg new Date() — UTC only -->
        <module name="Regexp">
            <property name="format" value="LocalDateTime\.now\(\s*\)"/>
            <property name="message" value="UTC required: use Instant.now() per CLAUDE.md, not LocalDateTime.now()."/>
            <property name="illegalPattern" value="true"/>
            <property name="ignoreComments" value="true"/>
        </module>

        <module name="Regexp">
            <property name="format" value="new\s+Date\(\s*\)"/>
            <property name="message" value="UTC required: use Instant.now() per CLAUDE.md, not new Date()."/>
            <property name="illegalPattern" value="true"/>
            <property name="ignoreComments" value="true"/>
        </module>

        <!-- ADR-007: BigDecimal must not be constructed from a double literal -->
        <module name="Regexp">
            <property name="format" value="new\s+BigDecimal\s*\(\s*\d+\.\d+"/>
            <property name="message" value="ADR-007: do not construct BigDecimal from a double literal. Use new BigDecimal(String) or BigDecimal.valueOf()."/>
            <property name="illegalPattern" value="true"/>
            <property name="ignoreComments" value="true"/>
        </module>

        <!-- CLAUDE.md: no field injection — @Autowired on fields banned -->
        <module name="Regexp">
            <property name="format" value="@Autowired\s*\n\s*private\s+\w"/>
            <property name="message" value="CLAUDE.md: field injection forbidden. Use constructor injection."/>
            <property name="illegalPattern" value="true"/>
            <property name="ignoreComments" value="true"/>
        </module>

        <module name="SuppressWarningsHolder"/>
    </module>
</module>
